export const TASK_PRIORITIES = Object.freeze({
  LOW: 0,
  MEDIUM: 1,
  HIGH: 2
})

export const TASK_PRIORITIES_LABELS = Object.freeze(['Low', 'Medium', 'High'])

export const DEFAULT_PAGINATION_SIZE = 10
export const DEFAULT_PAGINATION_PAGE = 1
